AWSConfigurationTemplateVersion: 1.1.0.0
EnvironmentConfigurationMetadata:
  DateModified: '1582476470074'
OptionSettings:
  AWSEBV2LoadBalancer.aws:elbv2:loadbalancer:
    AccessLogsS3Bucket: verity-access-logs
    AccessLogsS3Enabled: true
    AccessLogsS3Prefix: vbios-platform-test-test
  aws:elasticbeanstalk:application:environment:
    HOST: https://afteryoursurgery.com
    LANG: en_US.UTF-8
    MAILGUN_DOMAIN: mg.afteryoursurgery.com
    MAIL_HOST_DOMAIN: afteryoursurgery.com
    RACK_ENV: production
    RAILS_ENV: production
    RAILS_LOG_TO_STDOUT: enabled
    RAILS_SERVE_STATIC_FILES: enabled
    WEBPACKER_DEV_SERVER_HOST: webpack
